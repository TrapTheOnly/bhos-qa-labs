plugins {
    id "org.sonarqube" version "3.3"
}

sonarqube {
    properties {
        property "sonar.projectKey", "TrapTheOnly_bhos-qa-labs"
        property "sonar.organization", "traptheonly"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}